# Normalize text files to LF in the repo; Windows batch files must stay CRLF to run.
* text=auto eol=lf
*.bat text eol=crlf

# Binary assets — never touch line endings
*.glb binary
*.png binary
*.jpg binary
*.ogg binary
*.mp3 binary
*.wav binary
